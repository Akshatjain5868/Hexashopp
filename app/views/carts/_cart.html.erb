<% unless cart.line_items.empty? %>
	<div class="cart_title">Your Cart</div>
	<table>
		<%= render(cart.line_items) %>
		<tr>
			<td colspan="2">Total</td>
			<td class="total_cell"><%= number_to_currency(cart.total_price)  %></td>
		</tr>
	</table>

	<%# ye baad mei add kiya hai !! %>
	<%= session[:amount] = cart.total_price %>

	<%= button_to 'Empty Cart', cart, :method => :delete, :confirm => 'Are you sure?' %>
	<p>
		<%= link_to "Continue Shopping",:controller=>"gallery",:action=>"index" %> | 


		<%# <% if session[:user] %>
		<%# <%= link_to "Checkout" ,gallery_checkout_path,:amount=>cart.total_price %>	 
		<%= link_to "Checkout" ,:controller=>"gallery",:action=>"checkout",:amount=>cart.total_price %>
		<%# <% else %> 
		<%# <%= link_to "Checkout", :controller=>"account",:action=>"login"  %> 
		
		<%# <% end %> 
	</p>
<% end %>