<%= form_with(model: craft_store) do |form| %>
  <% if craft_store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(craft_store.errors.count, "error") %> prohibited this craft_store from being saved:</h2>

      <ul>
        <% craft_store.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :item_images %>
    <%= form.file_field :item_images , multiple: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
